{
	"compilerOptions": {
		"target": "ESNext",
		"lib": ["DOM", "ES2023"],
		"moduleResolution": "node"
	},

	"watchOptions": {
		"watchFile": "useFsEvents",
		"watchDirectory": "useFsEvents",
		"fallbackPolling": "dynamicPriority"
	}
}


// tsc --watch 